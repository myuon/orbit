type Pair(A: type, B: type) = struct {
  first: A,
  second: B,

  fun set_first(self: Pair(A, B), a: A): int do
    self.first = a;
    return 0;
  end
};

fun main() do
  let p = new Pair(int, int) { .first = 1, .second = 42 };
  p.set_first(3);

  return p.first + p.second;
end