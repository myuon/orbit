// Recursive Fibonacci benchmark
// Deep recursion performance test
fun fibonacci(n) do
  if n <= 1 then
    return n;
  else
    return fibonacci(n - 1) + fibonacci(n - 2);
  end
end

fun main() do
  return fibonacci(29);
end