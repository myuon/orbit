fun main() do
  let vec = new vec(int) {.capacity = 128}; // FIXME: no capacity
  let n = 0;
  while (n < 1000) do
    vec <- n * n;
    n = n + 1;
  end

  vec[777] = vec[777] + 10;

  return vec[777];
end
